#include<iostream>
#include<cstdio>
using namespace std;
int main()
{
int t,c,k,w;
scanf("%d", &t);
while(t--) 
{   scanf("%d",&c); 
	k=sqrt(c)
	for(int i=c;i<=)
	scanf("%d %d %d", &c, &k, &w);
    if(c*w <= k)
    printf("yes\n");
    else
    printf("no\n");
}
 
return 0;
}
