#include<iostream>
#include<cstdio>
using namespace std;
int main()
{
int t,c,k,w;
scanf("%d", &t);
while(t--) 
{   scanf("%d",&c); 
	k=sqrt(c);
	while(1)
	{   for(int j=2;j<=k;j++)
		{	if(c%j==0)  
			}
		}
	scanf("%d %d %d", &c, &k, &w);
    if(c*w <= k)
    printf("yes\n");
    else
    printf("no\n");
}
 
return 0;
}
