#include<iostream>
#include<cstdio>
#include<string>
#include<sstream>
#include<vector>
using namespace std;
int main()
{
int t,a,b,c; string tmp;
scanf("%d", &t);
vector<string> x;
while(t--)
{	
	getline(tmp,cin);
	x.push_back(tmp);
}
int sz=x.size();
for(int j=1;j<sz;j++)
{  
	for(int k=1;j<sz;j++)
	{
		for(int i=1;j<sz;j++)
		{ 
			if(substr(x[i]))
			}
		}
	} 
return 0;
}
